package ch06.problem;

public class BaseApp {
    public static void main(String args[]) {
        Base base = new Base();
        base.service("낮");
        base.service("밤");
        base.service("오후");
    }
}
